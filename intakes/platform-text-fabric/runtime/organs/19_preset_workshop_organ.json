{
  "id": "axm.text.organ.preset_workshop",
  "version": "0.6.0",
  "label": "Preset Workshop Organ",
  "purpose": "Lets humans create, import, replace, delete, and export local presets without modifying the built-in catalog.",
  "inputs": [
    "current_configuration",
    "imported_json",
    "local_storage"
  ],
  "outputs": [
    "custom_preset",
    "validation_messages",
    "export_json"
  ],
  "rules": [
    "Built-in presets remain immutable.",
    "Custom presets are local-only by default.",
    "Imported presets must reference known recipes and roles.",
    "Deletion only applies to custom presets."
  ]
}
