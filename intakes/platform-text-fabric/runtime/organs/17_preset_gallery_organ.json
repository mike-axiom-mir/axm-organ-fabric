{
  "id": "axm.text.organ.preset_gallery",
  "version": "0.5.0",
  "label": "Preset Gallery Organ",
  "purpose": "Packages safe combinations of semantic role, visual recipe, surface protection, and effect tuning into discoverable reusable presets.",
  "inputs": [
    "category",
    "search",
    "preset_id",
    "platform",
    "user_override"
  ],
  "outputs": [
    "resolved_preset",
    "base_request",
    "effect_tuning",
    "exportable_configuration"
  ],
  "rules": [
    "Presets are starting points, not hidden canon.",
    "Every preset remains inspectable and individually overridable.",
    "Preset application may not bypass accessibility and glitch safety organs.",
    "Custom tuning is exported explicitly rather than silently replacing the source preset."
  ]
}
