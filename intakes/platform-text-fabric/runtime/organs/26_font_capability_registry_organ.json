{
  "id": "axm.text.organ.font_capability_registry",
  "version": "0.9.0",
  "label": "Font Capability Registry Organ",
  "purpose": "Builds local metadata registries for project fonts, including names, hashes, Unicode coverage, variable axes, shaping tables, color-font support, and licensing metadata.",
  "inputs": [
    "font_paths",
    "recursive",
    "redact_paths"
  ],
  "outputs": [
    "font_registry",
    "capability_summary",
    "errors"
  ],
  "rules": [
    "Registries contain metadata and hashes only.",
    "Paths can be redacted for portable reports.",
    "Font licensing remains the responsibility of the project owner."
  ]
}
