{
  "id": "axm.text.organ.font_fallback_planner",
  "version": "0.9.0",
  "label": "Font Fallback Planner Organ",
  "purpose": "Selects a minimal ordered set of locally registered fonts that collectively covers the requested text.",
  "inputs": [
    "font_registry",
    "text"
  ],
  "outputs": [
    "font_stack",
    "missing_characters",
    "coverage_complete"
  ],
  "rules": [
    "Selection is deterministic and greedy.",
    "The plan never copies font binaries.",
    "Metric harmony and visual compatibility still require human or target-runtime review."
  ]
}
