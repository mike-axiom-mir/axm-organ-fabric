{
  "id": "axm.text.organ.batch_library_compiler",
  "version": "0.7.0",
  "label": "Batch Library Compiler Organ",
  "purpose": "Compiles multiple presets into a navigable, verified platform library for reuse across many games, apps, and software surfaces.",
  "inputs": [
    "preset_ids",
    "category",
    "featured_only",
    "platforms",
    "quality_tier"
  ],
  "outputs": [
    "compiled_preset_bundles",
    "library_index",
    "library_manifest",
    "preview_links"
  ],
  "rules": [
    "Each preset remains independently versioned and verifiable.",
    "Batch compilation may filter by category or featured status.",
    "A failure must stop the library build rather than silently skip a requested preset."
  ]
}
